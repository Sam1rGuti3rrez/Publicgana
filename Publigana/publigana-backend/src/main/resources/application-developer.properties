# ─────────────────────────────────────────────
# Perfil: developer — desarrollo local
# ─────────────────────────────────────────────

# Base de datos local
spring.datasource.url=jdbc:postgresql://localhost:5432/publigana
spring.datasource.username=postgres
spring.datasource.password=b97041300306

# SQL visible en consola (solo en desarrollo)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging Flyway detallado en desarrollo
logging.level.org.flywaydb=DEBUG

# Puerto local
server.port=8080

# CORS — solo el frontend de desarrollo
cors.allowed-origins=http://localhost:5173,http://127.0.0.1:5173,http://localhost:4173
